using Microsoft.Tools.ServiceModel.SvcUtil;
using System;
using System.Collections.Generic;
using System.Reflection;

class Program
{
    static void Main(string[] args)
    {
        //Options options = Options.ParseArguments(args);
        //XmlSerializerGenerator generator = new XmlSerializerGenerator(options);
        //var assembly = typeof(WCFClient.ServiceReference1.IService1).Assembly;
        //var name = assembly.FullName;
        //Assembly.Load(name);
        //generator.GenerateCode(new List<Assembly>() { assembly });
    }
}
